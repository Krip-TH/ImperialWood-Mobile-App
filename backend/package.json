{
  "name": "imperialwood-api",
  "version": "1.0.0",
  "private": true,
  "description": "ImperialWood Express REST API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check config/database.js && node --check routes/auth.js && node --check routes/products.js && node --check routes/categories.js && node --check routes/stores.js && node --check routes/favorites.js && node --check routes/cart.js && node --check routes/orders.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.12.0"
  }
}
